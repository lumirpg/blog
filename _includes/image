{% capture imagePath %}{{ page.date | date: "%Y/%-m" }}/{{ include.name }}{% endcapture %}
{% if include.caption %}
  <figure>
    <img src="{{ site.url }}/{% if site.baseurl %}{{ site.baseurl }}{% endif %}assets/posts/{{ imagePath }}" {% if include.alt %} alt="{{ include.alt }}" {% endif %} {% if include.width %} width="{{ include.width }}" {% endif %}/>
    <figcaption>{{ include.caption }}</figcaption>
  </figure>
{% else %}
  <img src="{{ site.url }}/{% if site.baseurl %}{{ site.baseurl }}{% endif %}assets/posts/{{ imagePath }}" {% if include.alt %} alt="{{ include.alt }}" {% endif %} {% if include.width %} width="{{ include.width }}" {% endif %}>
{% endif %}
